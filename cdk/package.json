{
  "name": "cdk",
  "version": "0.1.0",
  "bin": {
    "cdk": "bin/cdk.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy:dev": "cd .. && cd backend && cd app && cp config.php config-old.php && cp config-dev.php config.php && cd .. && cd .. && cd cdk && NO_PREBUILT_LAMBDA=1 cdk deploy --all --profile hotelinking-dev --context env=dev && cd .. && cd backend && cd app && cp config-old.php config.php && rm config-old.php",
    "deploy:prod": "cd .. && cd backend && cd app  && cp config.php config-old.php && cp config-prod.php config.php && cd .. && cd .. && cd cdk && NO_PREBUILT_LAMBDA=1 cdk deploy --all --profile hotelinking-prod --context env=prod && cd .. && cd backend && cd app && cp config-old.php config.php && rm config-old.php",
    "synth:dev": "cdk synth --context env=dev --no-staging",
    "synth:prod": "cdk synth --context env=prod --no-staging"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "20.14.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "aws-cdk": "2.153.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.5.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.153.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}