{
	admin off

  	frankenphp {
    	num_threads 8
    	max_threads auto
		max_wait_time 10s
  	}
}

localhost:80 {
	root * /app
	encode zstd gzip
	php_server
}

localhost:443 {
	tls {
    	issuer internal
  	}

  	root * /app
	encode zstd gzip
  	php_server 
}